{
  "name": "cricviz-dashboard",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"cd frontend && npm run dev:ui\" \"cd backend && uvicorn main:app --reload\" \"cd backend && celery -A worker.celery_app worker --pool=solo --loglevel=info\""
  }
}
